import { State } from '../../store/BasicState';

// find the implementation  in /store/binary-tree-preorder-traveral/Card
const update = (value: number, state: State): State => ({ ...state });
export default update;
